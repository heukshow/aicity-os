# API origin only, without /api/analyze. Public build-time value; never a secret.
# Local API: http://localhost:8000
# Leave blank until a backend is available. Rebuild Next.js after changing it.
NEXT_PUBLIC_SHEETPROOF_API_URL=
